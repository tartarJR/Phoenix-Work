defmodule TaksoWeb.BidController do
  use TaksoWeb, :controller

  def summary(conn, _params) do


  end

  def create(conn, %{"item_id" => item_id, "bidding" => user_bid, "user" => user}) do

  end

  def bids(conn, %{"item_id" => item_id}) do

  end

end
